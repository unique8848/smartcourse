apply plugin: 'idea'

idea {
    project {
        jdkName = '1.7'
        languageLevel = 'JDK_1_7'

        ipr {
            withXml { provider ->
                provider.node.component.find { it.@name == 'VcsDirectoryMappings' }.mapping.@vcs = 'Git'
            }
        }
    }

}

